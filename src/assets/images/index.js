const images = {
    logo: require('./logo.svg').default,
    messageIcon: require('./message.svg').default,
    noticeIcon: require('./notice.svg').default,
    noImage: require('./no-image.png'),
}

export default images